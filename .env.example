# Copy this file to .env.local (gitignored) and fill in the values below.
# Project ID and dataset are public identifiers, not secrets, but only
# this .example file is committed — the real .env.local stays local.

VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=
