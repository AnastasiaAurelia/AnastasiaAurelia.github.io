# Copy this file to scripts/.env.migration (gitignored) before running the
# migration script. Never commit the real file, print its contents, or
# paste the token into chat/source code.
#
# Get a token: https://www.sanity.io/manage/project/l3uxv1lk/api#tokens
# "Editor" permission is enough — it doesn't need admin/deploy rights.
#
# Run with:
#   node --env-file=scripts/.env.migration scripts/migrate-to-sanity.ts

SANITY_WRITE_TOKEN=
SANITY_STUDIO_PROJECT_ID=l3uxv1lk
SANITY_STUDIO_DATASET=production
