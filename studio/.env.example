# Copy this file to .env (gitignored) and fill in the values below.
# Project ID and dataset are public identifiers, not secrets — but the
# actual .env file still isn't committed, to keep one clean convention
# for every env file in this repo.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=
